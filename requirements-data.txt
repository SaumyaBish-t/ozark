# Karna — Sprint 3+ market-data & trading-analytics stack.
# Install once we reach Sprint 3 (market data integration).
#
# Heads-up on Python 3.13:
#   - numpy 2.x and pandas 2.2.3 have working 3.13 wheels (no compilation).
#   - pandas-ta 0.3.14b0 (the only PyPI release) is broken on numpy 2.x
#     because it imports the removed `np.NaN`. We work around that by
#     pinning the patched master branch from GitHub.
#   - If anything here gives you trouble, drop to Python 3.12 — the entire
#     scientific-Python stack is rock solid there.

# ---- Sprint 3: market data ----
numpy==2.1.3
pandas==2.2.3
jugaad-data==0.27
nsetools==1.0.11
# yfinance is intentionally NOT pinned here — jugaad-data 0.27 hard-pins
# beautifulsoup4==4.9.3, which conflicts with yfinance's >=4.11.1 demand.
# When we need a yfinance fallback (Sprint 4+), install it AFTER everything
# else, ignoring deps:
#     pip install --no-deps yfinance==0.2.43
# yfinance works fine with the bs4 4.9.x that jugaad-data brings in;
# it just refuses to install in the normal resolver because of the pin.

# Technical indicators. The original twopirllc/pandas-ta repo was taken
# down in 2025; pandas-ta-classic is the actively maintained community
# fork with numpy 2.x compatibility fixed. Same API surface (import as
# `pandas_ta`), so our indicator wrappers don't change.
pandas-ta-classic==0.6.20

# ---- Sprint 3+: browser ops ----
playwright==1.47.0

# ---- Sprint 5+: dashboard + API ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
plotly==5.24.1
jinja2==3.1.4
