# Karna — Sprint 1 + 2 dependencies.
# All pure-Python or wheel-only, so this installs fast on Python 3.11 / 3.12 / 3.13.
# Sprint 3+ data stack (numpy/pandas/pandas-ta/yfinance/...) lives in
# requirements-data.txt — install it when we get there.

# ---- Sprint 1: core agent + Telegram + concept graph ----
python-telegram-bot==21.6
ollama==0.3.3
neo4j==5.24.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
rich==13.8.1

# ---- Sprint 2: semantic memory + scheduling ----
# chromadb pulls in numpy >= 1.22 transitively, but it picks numpy 2.x on
# Python 3.13 which has pre-built wheels. No source build needed.
chromadb==0.5.23
sentence-transformers==3.2.1
apscheduler==3.10.4
pyyaml==6.0.2

# ---- Optional fallback LLM (used from Sprint 1 when complex reasoning needed) ----
openai==1.51.2

# ---- Orchestration (introduced when agent graph gets complex; ok to have now) ----
langgraph==0.2.34
langchain-core==0.3.10

# ---- Dev / test ----
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
